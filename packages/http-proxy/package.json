{
  "name": "http-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "hiproxy start --port 8008 --pac-proxy ./",
    "start_window": "hiproxy start --open --port 8008 --pac-proxy ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "hiproxy": "^1.3.3"
  }
}
